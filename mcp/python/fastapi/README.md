# python/fastapi

MCP examples for [tiangolo/fastapi](https://github.com/tiangolo/fastapi).

Add here:

- `analyze_architecture.md` — Human-readable summary
- `analyze_architecture.json` — (Optional) Request + response
- `check_cycles.md` — Human-readable summary for check_cycles

Use the same format as `mcp/typescript/express/`.
