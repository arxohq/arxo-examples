# Config for express (SCC report). Use: arxo analyze --path <clone> --config this file.
data:
  language: auto
  import_graph:
    group_by: file
    exclude:
      - node_modules
      - .yarn
      - dist
      - build
      - .next

metrics:
  - id: scc
